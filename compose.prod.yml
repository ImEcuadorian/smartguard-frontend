services:
  frontend:
    image: ghcr.io/imecuadorian/smartguard-frontend:${SMARTGUARD_VERSION:-0.1.0}
    container_name: smartguard-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
